# drivers_minCCF.pdf recovered from Keynote. No generating export or equivalent
# variable-constructed filename was found in the supplied R scripts/helpers.
run_SupplementaryFig2 <- function(input_dir,output_dir) {
  list('Supplementary Fig. 2'=ptc_blocked('Supplementary Fig. 2',
    'Minimum detected CCF formula, alteration grouping and drivers_minCCF.pdf generator are absent. Coverage/purity metadata are insufficient to recover the threshold model.','unresolved'))
}
